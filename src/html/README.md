This directory contain html files.

this directory represented as root of the server.
the sub-directory will be threated as pages.

for example about pages `html/about/index.html` can be accessed
at `/about`.